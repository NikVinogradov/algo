from collections import defaultdict
from typing import Optional

class Solution:
    def isPalindrome(self, head:Optional[ListNode]) -> bool:
        найти середину, перевернуть вторую часть и потом поэлеменнтно сравнить
    
    O(n) и O(1)
